export function divObj(startX,startY,height,width,color,id){
    this.id = id
    this.left = startX;
    this.top = startY;
    this.height = height;
    this.width = width;
    this.backgroundColor = color;
}